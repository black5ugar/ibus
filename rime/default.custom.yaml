patch:
  schema_list:
    - schema: luna_pinyin
  style:
    horizontal: true

  "menu/page_size": 7  # 候选项分页大小
__patch:
# Rx: emoji:customize:schema=luna_pinyin {
  - patch/+:
      __include: emoji_suggestion:/patch
# }
